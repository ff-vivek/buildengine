name: buildengine_server
description: BuildEngine Server API implementation in Dart
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  shelf_multipart: ^0.1.0
  shelf_static: ^1.1.1
  uuid: ^4.2.1
  mime: ^1.0.4
  path: ^1.8.3
  http: ^1.1.0
  logging: ^1.2.0
  sqlite3: ^2.4.0
  googleapis: ^11.4.0
  googleapis_auth: ^1.4.1
  archive: ^3.4.10

dev_dependencies:
  test: ^1.24.6
  lints: ^3.0.0
  build_runner: ^2.4.7

executables:
  buildengine_server:
